diff --git a/node_modules/.bun/@browserbasehq+stagehand@3.2.0+685ac8eae13b5a19/node_modules/@browserbasehq/stagehand/.bun-tag-5c24b4f7979d9323 b/.bun-tag-5c24b4f7979d9323
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/cjs/lib/v3/llm/aisdk.js b/dist/cjs/lib/v3/llm/aisdk.js
index 52ec5eb0a6e65d41e809ec6af96a5b4d986a2ea4..8f00bc4c63a816eb1448ea33b7faba54652580aa 100644
--- a/dist/cjs/lib/v3/llm/aisdk.js
+++ b/dist/cjs/lib/v3/llm/aisdk.js
@@ -107,7 +107,7 @@ class AISdkClient extends LLMClient_js_1.LLMClient {
         const temperature = isKimi ? 1 : options.temperature;
         // Models that lack native structured-output support need a prompt-based
         // JSON fallback instead of response_format: { type: "json_schema" }.
-        const PROMPT_JSON_FALLBACK_PATTERNS = ["deepseek", "kimi", "glm"];
+        const PROMPT_JSON_FALLBACK_PATTERNS = ["deepseek", "kimi", "glm", "stepfun"];
         const needsPromptJsonFallback = PROMPT_JSON_FALLBACK_PATTERNS.some((p) => this.model.modelId.includes(p));
         if (options.response_model) {
             // Log LLM request for generateObject (extract)
@@ -130,24 +130,73 @@ You must respond in JSON format. respond WITH JSON. Do not include any other tex
                 });
             }
             try {
-                objectResponse = await (0, ai_1.generateObject)({
-                    model: this.model,
-                    messages: formattedMessages,
-                    schema: options.response_model.schema,
-                    temperature,
-                    providerOptions: isGPT5
-                        ? {
-                            openai: {
-                                textVerbosity: isCodex ? "medium" : "low", // codex models only support 'medium'
-                                reasoningEffort: isCodex
-                                    ? "medium"
-                                    : usesLowReasoningEffort
-                                        ? "low"
-                                        : "minimal",
+                if (needsPromptJsonFallback) {
+                    const textResponse = await (0, ai_1.generateText)({
+                        model: this.model,
+                        messages: formattedMessages,
+                        temperature,
+                    });
+                    let raw = (textResponse.text ?? "").trim();
+                    const fenceMatch = raw.match(/^```(?:json)?\s*([\s\S]*?)```$/m);
+                    if (fenceMatch) {
+                        raw = fenceMatch[1].trim();
+                    }
+                    const parsedJson = JSON.parse(raw);
+                    if (parsedJson &&
+                        typeof parsedJson === "object" &&
+                        !Array.isArray(parsedJson) &&
+                        Object.keys(parsedJson).length === 0) {
+                        objectResponse = {
+                            object: {
+                                elementId: "0-0",
+                                description: "No actionable element (empty JSON from model)",
+                                method: "click",
+                                arguments: [],
+                                twoStep: false,
                             },
+                            usage: textResponse.usage,
+                            finishReason: textResponse.finishReason,
+                        };
+                    }
+                    else {
+                        const parseResult = options.response_model.schema.safeParse(parsedJson);
+                        if (!parseResult.success) {
+                            throw new ai_1.NoObjectGeneratedError({
+                                message: `Model returned JSON that failed schema validation: ${parseResult.error.message}`,
+                                cause: parseResult.error,
+                                text: textResponse.text,
+                                response: textResponse.response,
+                                usage: textResponse.usage,
+                                finishReason: textResponse.finishReason,
+                            });
                         }
-                        : undefined,
-                });
+                        objectResponse = {
+                            object: parseResult.data,
+                            usage: textResponse.usage,
+                            finishReason: textResponse.finishReason,
+                        };
+                    }
+                }
+                else {
+                    objectResponse = await (0, ai_1.generateObject)({
+                        model: this.model,
+                        messages: formattedMessages,
+                        schema: options.response_model.schema,
+                        temperature,
+                        providerOptions: isGPT5
+                            ? {
+                                openai: {
+                                    textVerbosity: isCodex ? "medium" : "low", // codex models only support 'medium'
+                                    reasoningEffort: isCodex
+                                        ? "medium"
+                                        : usesLowReasoningEffort
+                                            ? "low"
+                                            : "minimal",
+                                },
+                            }
+                            : undefined,
+                    });
+                }
             }
             catch (err) {
                 // Log error response to maintain request/response pairing
diff --git a/dist/esm/lib/v3/llm/aisdk.js b/dist/esm/lib/v3/llm/aisdk.js
index 060afa49050bc1ccb925105fef121c099d5ad925..a07e1fe549086131902365f70164093d15d5711a 100644
--- a/dist/esm/lib/v3/llm/aisdk.js
+++ b/dist/esm/lib/v3/llm/aisdk.js
@@ -104,7 +104,7 @@ export class AISdkClient extends LLMClient {
         const temperature = isKimi ? 1 : options.temperature;
         // Models that lack native structured-output support need a prompt-based
         // JSON fallback instead of response_format: { type: "json_schema" }.
-        const PROMPT_JSON_FALLBACK_PATTERNS = ["deepseek", "kimi", "glm"];
+        const PROMPT_JSON_FALLBACK_PATTERNS = ["deepseek", "kimi", "glm", "stepfun"];
         const needsPromptJsonFallback = PROMPT_JSON_FALLBACK_PATTERNS.some((p) => this.model.modelId.includes(p));
         if (options.response_model) {
             // Log LLM request for generateObject (extract)
@@ -127,24 +127,73 @@ You must respond in JSON format. respond WITH JSON. Do not include any other tex
                 });
             }
             try {
-                objectResponse = await generateObject({
-                    model: this.model,
-                    messages: formattedMessages,
-                    schema: options.response_model.schema,
-                    temperature,
-                    providerOptions: isGPT5
-                        ? {
-                            openai: {
-                                textVerbosity: isCodex ? "medium" : "low", // codex models only support 'medium'
-                                reasoningEffort: isCodex
-                                    ? "medium"
-                                    : usesLowReasoningEffort
-                                        ? "low"
-                                        : "minimal",
+                if (needsPromptJsonFallback) {
+                    const textResponse = await generateText({
+                        model: this.model,
+                        messages: formattedMessages,
+                        temperature,
+                    });
+                    let raw = (textResponse.text ?? "").trim();
+                    const fenceMatch = raw.match(/^```(?:json)?\s*([\s\S]*?)```$/m);
+                    if (fenceMatch) {
+                        raw = fenceMatch[1].trim();
+                    }
+                    const parsedJson = JSON.parse(raw);
+                    if (parsedJson &&
+                        typeof parsedJson === "object" &&
+                        !Array.isArray(parsedJson) &&
+                        Object.keys(parsedJson).length === 0) {
+                        objectResponse = {
+                            object: {
+                                elementId: "0-0",
+                                description: "No actionable element (empty JSON from model)",
+                                method: "click",
+                                arguments: [],
+                                twoStep: false,
                             },
+                            usage: textResponse.usage,
+                            finishReason: textResponse.finishReason,
+                        };
+                    }
+                    else {
+                        const parseResult = options.response_model.schema.safeParse(parsedJson);
+                        if (!parseResult.success) {
+                            throw new NoObjectGeneratedError({
+                                message: `Model returned JSON that failed schema validation: ${parseResult.error.message}`,
+                                cause: parseResult.error,
+                                text: textResponse.text,
+                                response: textResponse.response,
+                                usage: textResponse.usage,
+                                finishReason: textResponse.finishReason,
+                            });
                         }
-                        : undefined,
-                });
+                        objectResponse = {
+                            object: parseResult.data,
+                            usage: textResponse.usage,
+                            finishReason: textResponse.finishReason,
+                        };
+                    }
+                }
+                else {
+                    objectResponse = await generateObject({
+                        model: this.model,
+                        messages: formattedMessages,
+                        schema: options.response_model.schema,
+                        temperature,
+                        providerOptions: isGPT5
+                            ? {
+                                openai: {
+                                    textVerbosity: isCodex ? "medium" : "low", // codex models only support 'medium'
+                                    reasoningEffort: isCodex
+                                        ? "medium"
+                                        : usesLowReasoningEffort
+                                            ? "low"
+                                            : "minimal",
+                                },
+                            }
+                            : undefined,
+                    });
+                }
             }
             catch (err) {
                 // Log error response to maintain request/response pairing
